start
 numbers: x,y,z;   //error numbers instead of number
 number: max;
 situation( x > y)
	situation ( x> z)
		max <- x;
	other max <- z;
 other
  situation ( y>z)
   	max <- y;
  other max <- z;



 number: min;
 situation( x < y)
	situation ( x< z)
		min <- x;
	other min <- z;
 other
  situation ( y<z)
   	min <- y;
  other min <- z;

number: diff;
diff <- max - min ;